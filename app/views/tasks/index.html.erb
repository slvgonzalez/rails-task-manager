<h3>All my tasks: </h3>
<ul>
  <% @tasks.each do |task| %>
    <li><i class="fa-solid fa-check"></i><%= link_to task.title, task_path(task.id) %></li>
  <% end %>
</ul>
<i class="fa-solid fa-check"></i>

<%= link_to "➕ Add a new task", new_task_path %>
